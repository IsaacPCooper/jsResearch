// Variables for two cards
let firstCard = 2;
let secondCard = 4;

// Sum of both cards
let sum = firstCard + secondCard;

// if condition
if (sum < 21) {
    // Draw a card
} else if (sum === 21){
    // YOU WIN!
}
    // You lose!